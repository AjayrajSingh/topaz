# Copyright 2016 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: gallery
description: View UI Widgets and screens.
dependencies:
  calendar:
    path: ../../calendar/modules/calendar
  chat:
    path: ../../chat/modules/chat
  config_flutter:
    path: ../packages/config_flutter
  contacts:
    path: ../../contacts/modules/contacts
  flutter:
    sdk: flutter
  flutter_markdown:
    sdk: flutter
  models:
    path: ../packages/models
  widgets:
    path: ../packages/widgets
  widget_specs:
    path: ../tools/widget_specs
flutter:
  uses-material-design: true
  assets:
    - assets/config.json
